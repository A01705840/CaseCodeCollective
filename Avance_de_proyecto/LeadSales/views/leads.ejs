<%- include('includes/head.ejs', {
    username: username,
    permisos: permisos}) %>
    <input type="hidden" id="_csrf" name="_csrf" value="<%= csrfToken %>">
 
    <div id="modal" onclick="cerrarModal()" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background-color: rgba(0, 0, 0, 0.3);" class="flex items-center justify-center">
        <div onclick="event.stopPropagation()" class="relative transform overflow-hidden rounded-lg bg-gray-100 text-left shadow-xl transition-all" style="width: 600px; height: 580px;">
            <h1 class="text-xl font-sans text-ellipsis capitalize dark:text-white" style="padding-left: 25px; padding-top: 25px;">Modificar Lead</h1>
            <section class="max-w-4xl p-6 mx-auto bg-white shadow-md dark:bg-gray-800 ">
                <form>
                    <div class="grid grid-cols-1 gap-6  sm:grid-cols-2">
                        <div>
                            <label class="text-ellipsis" for="nombre">Nombre:</label>
                            <input type="text" id="nombre" name="nombre" required class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                            <span id="nombreError" class="error text-red-500 text-xs italic" style="position:fixed;"></span>
                        </div>
    
                        <div>
                            <label class="text-ellipsis" for="fecha">Fecha del Primer Mensaje:</label>
                            <input type="date" id="fecha" required class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                        </div>
    
                        <div>
                            <label class="text-ellipsis" >Asignado a:</label>
                            <select id="asignado_a" name="asignado_a" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var asignadosUnicos = [...new Set(leads.map(lead => lead.asignado_a))];
                                asignadosUnicos.sort(); // Ordena los asignados
                                asignadosUnicos.forEach(function(asignado) { %> // Itera sobre los asignados únicos
                                    <option value="<%= asignado %>"><%= asignado %></option> // Crea una opción con el asignado
                                <% }); %>
                            </select>
                        </div>

                        <div>
                            <label class="text-ellipsis" >Embudo:</label>
                            <select id="embudo" name="embudo" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var embudosUnicos = [...new Set(leads.map(lead => lead.Embudo))];
                                embudosUnicos.sort(); 
                                embudosUnicos.forEach(function(embudo) { %> 
                                    <option value="<%= embudo %>"><%= embudo %></option> 
                                <% }); %>
                            </select>
                        </div>
    
                        <div>
                            <label class="text-ellipsis" >Etapa:</label>
                            <select id="etapa" name="etapa" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var etapasUnicas = [...new Set(leads.map(lead => lead.Etapa))];
                                etapasUnicas.sort(); 
                                etapasUnicas.forEach(function(etapa) { %>
                                    <option value="<%= etapa %>"><%= etapa %></option>
                                <% }); %>
                            </select>
                        </div>
    
                        <div>
                            <label class="text-ellipsis" >Status:</label>
                            <select id="status" name="status" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var statusUnicos = [...new Set(leads.map(lead => lead.Status))];
                                statusUnicos.sort(); // Ordena los status
                                statusUnicos.forEach(function(status) { %>
                                    <option value="<%= status %>"><%= status %></option>
                                <% }); %>
                            </select>
                        </div>
    
                        <div>
                            <label class="text-ellipsis" for="telefono">Teléfono:</label>
                            <input type="text" id="telefono" name="telefono" required class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                            <span id="telefonoError" class="error text-red-500 text-xs italic" style="position: absolute;"></span>
                        </div>
    
                        <div>
                            <label class="text-ellipsis" >Creado Manual:</label>
                            <select id="creadomanual" name="creadomanual" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <option value="NO">NO</option>
                                <option value="SI">SI</option>
                            </select>
                        </div>
                        <div>
                            <label class="text-ellipsis" >Archivado:</label>
                            <select id="archivado" name="archivado" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <option value="NO">NO</option>
                                <option value="SI">SI</option>
                            </select>
                            <button id="botonGuardar"onclick="guardarCambios()" class="bg-blue-500 hover:bg-blue-700 text-white font-bold" style="padding: 10px 24px; text-align: center; text-decoration: none; display: inline-block; font-size: 16px; margin: 40px auto 0px; cursor: pointer; position: relative;  left: 338px; bottom: 85px; transition: background-color 0.3s ease; border-radius: 10px;">Guardar cambios</button>
                        </div>

                        <div class="flex justify-end mt-6">
                            <button type="button" onclick="cerrarModal()" class="hover:text-blue-700 text-ellipsis" style="border: none; padding: 0px 10px; text-align: center; text-decoration: none; display: inline-block; font-size: 35px; margin: 0; cursor: pointer; position: absolute; top: 5px; right: 20px; transition: background-color 0.3s ease; border-radius: 50%;">x</button>
                        </div>
                    </div>
                </form>
            </section>
        </div>
    </div>
    
    <div id="modalCrear" onclick="cerrarModalCrear()" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background-color: rgba(0, 0, 0, 0.3);" class="flex items-center justify-center">
        <div onclick="event.stopPropagation()" class="relative transform overflow-hidden rounded-lg bg-indigo-600 text-left shadow-xl transition-all" style="width: 600px; height: 580px;">
            <h1 class="text-xl font-bold text-white capitalize dark:text-white" style="padding-left: 20px; padding-top: 20px;">CREAR LEAD</h1>
            <section class="max-w-4xl p-6 mx-auto bg-indigo-600 rounded-md shadow-md dark:bg-gray-800 ">
                <form>
                    <div class="grid grid-cols-1 gap-6 mt-4 sm:grid-cols-2">
                        <div>
                            <label class="text-white dark:text-gray-200" for="nombre">Nombre:</label>
                            <input type="text" id="nombreCrear" name="nombre" required class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                            <span id="nombreErrorCrear" class="error text-red-200 text-xs italic" style="position:fixed;"></span>
                        </div>
    
                        <div>
                            <label class="text-white dark:text-gray-200" for="fecha">Fecha del Primer Mensaje:</label>
                            <input type="date" id="fechaCrear" required class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                        </div>
    
                        <div>
                            <label class="text-white dark:text-gray-200" >Asignado a:</label>
                            <select id="asignado_aCrear" name="asignado_a" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var asignadosUnicos = [...new Set(leads.map(lead => lead.asignado_a))];
                                asignadosUnicos.sort(); // Ordena los asignados
                                asignadosUnicos.forEach(function(asignado) { %> // Itera sobre los asignados únicos
                                    <option value="<%= asignado %>"><%= asignado %></option> // Crea una opción con el asignado
                                <% }); %>
                            </select>
                        </div>

                        <div>
                            <label class="text-white dark:text-gray-200" >Embudo:</label>
                            <select id="embudoCrear" name="embudo" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var embudosUnicos = [...new Set(leads.map(lead => lead.Embudo))];
                                embudosUnicos.sort(); 
                                embudosUnicos.forEach(function(embudo) { %> 
                                    <option value="<%= embudo %>"><%= embudo %></option> 
                                <% }); %>
                            </select>
                        </div>
    
                        <div>
                            <label class="text-white dark:text-gray-200" >Etapa:</label>
                            <select id="etapaCrear" name="etapa" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var etapasUnicas = [...new Set(leads.map(lead => lead.Etapa))];
                                etapasUnicas.sort(); 
                                etapasUnicas.forEach(function(etapa) { %>
                                    <option value="<%= etapa %>"><%= etapa %></option>
                                <% }); %>
                            </select>
                        </div>
    
                        <div>
                            <label class="text-white dark:text-gray-200" >Status:</label>
                            <select id="statusCrear" name="status" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <% 
                                var statusUnicos = [...new Set(leads.map(lead => lead.Status))];
                                statusUnicos.sort(); // Ordena los status
                                statusUnicos.forEach(function(status) { %>
                                    <option value="<%= status %>"><%= status %></option>
                                <% }); %>
                            </select>
                        </div>
    
                        <div>
                            <label class="text-white dark:text-gray-200" for="telefono">Teléfono:</label>
                            <input type="text" id="telefonoCrear" name="telefono" required class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                            <span id="telefonoErrorCrear" class="error text-red-200 text-xs italic" style="position: absolute;"></span>
                        </div>
    
                        <div>
                            <label class="text-white dark:text-gray-200" >Creado Manual:</label>
                            <select id="creadomanualCrear" name="creadomanual" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <option value="NO">NO</option>
                                <option value="SI">SI</option>
                            </select>
                        </div>
                        <div>
                            <label class="text-white dark:text-gray-200" >Archivado:</label>
                            <select id="archivadoCrear" name="archivado" class="block w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
                                <option value="NO">NO</option>
                                <option value="SI">SI</option>
                            </select>
                            <button id="botonGuardarCrear"onclick="guardarCambioscrear()" class="px-6 py-2 leading-5 text-white transition-colors duration-200 transform bg-pink-500 rounded-md hover:bg-pink-700 focus:outline-none focus:bg-gray-600" style="position: relative; left: 338px; bottom: 36px;">Guardar cambios</button>
                        </div>

                        <div class="flex justify-end mt-6">
                            <button type="button" onclick="cerrarModalCrear()" class="hover:text-black text-white" style="border: none; padding: 10px 20px; text-align: center; text-decoration: none; display: inline-block; font-size: 25px; margin: 0; cursor: pointer; position: absolute; top: 10px; right: 20px; transition: background-color 0.3s ease; border-radius: 50%;">X</button>
                        </div>
                    </div>
                </form>
            </section>
        </div>
    </div>


    

  
    <div class="p-4 sm:ml-20 sm:mr-10 font-sans">
      <div class="shadow-lg flex flex-col justify-right p-4 border-2 border-gray-200 rounded-lg dark:border-gray-700 bg-slate-200">
        <div class="inline-flex justify-between items-center mb-4">
            <span style='font-size:30px;'>📋 Leads</span>
              <div class="flex space-x-2">
                  <button onclick="event.preventDefault(); crear()" class="bg-white hover:bg-indigo-600 hover:text-white py-2 px-4 rounded-full transition duration-300">
                    Crear Lead</button>
                  <button class="bg-white hover:bg-indigo-600 hover:text-white py-2 px-4 rounded-full transition duration-300">Descargar Leads</button>
              </div>

            </div>
            <div class="flex justify-center sm:mt-4 p-1 border-1 border-gray-200 rounded-lg dark:border-gray-200 bg-white">
            <div class="container justify-center border-1 border-gray-200 rounded-lg dark:border-gray-200 bg-white">
                <% if(leads.length === 0) {%>
                    <p class="text-2xl text-gray-500 mt-48 mb-48 flex items-center justify-center">Importa tus primeros leads, en el historial, para comenzar...</p>
                <%  } else{ %> 
                    <div class="container flex flex-col items-center h-full">

                        <div class="container">
                            <table class="text-sm text-left text-gray-500 dark:text-gray-400 w-full">
                                <thead class=" flex w-full text-xs text-gray-700 uppercase bg-gray-100 dark:text-gray-500">
                                    <tr class="flex w-full">
                                        <th class="p-4 w-1/5">Lead</th>
                                        <th class="p-4 w-1/5">Fecha</th>
                                        <th class="p-4 w-1/5">Creador</th>
                                        <th class="p-4 w-1/5">Modificar</th>
                                        <th class="p-4 w-1/5">Eliminar</th>
                                    </tr>
                                </thead>
                            <!-- Remove the nasty inline CSS fixed height on production and replace it with a CSS class — this is just for demonstration purposes! -->
                                <tbody  id="table_content" class="bg-grey-light flex flex-col items-center justify-between overflow-y-scroll w-full" style="height: 64vh;">
                                    <% let row=0; for (let lead of leads.reverse()) { %>
                                    <% if(row%2==0){%>
                                    <tr class="flex w-full mb-4">
                                        <td class="p-4 w-1/5">
                                            <% if (lead.NombreLead==""){%>
                                               <p class="text-gray-400">Lead sin nombre</p>
                                            <% }else{%>
                                                <%= lead.NombreLead %>
                                            <% }%>
                                        </td>
                                        <td class="p-4 w-1/5">
                                            <%  let fechaActual = lead.FechaPrimerMensaje;
                                            // Convertir la fecha a un objeto Date
                                            const fecha = new Date(fechaActual);
                                            // Obtener día, mes y año
                                            const dia = fecha.getDate().toString().padStart(2, '0');
                                            const mes = (fecha.getMonth() + 1).toString().padStart(2, '0'); // El mes comienza desde 0
                                            const año = fecha.getFullYear();
                                            // Crear la cadena en el formato deseado (dd/mm/yyyy)
                                            const fechaFormateada = `${dia}/${mes}/${año}`;%>
                                            <%= fechaFormateada %>

                                        </td>
                                        <td class="p-4 w-1/5">
                                            <%= lead.asignado_a %>
                                        </td>
                                        <td class="p-4 w-1/5">
                                            <button onclick="event.preventDefault(); modificar(<%=lead.IDLead%>)" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-4 rounded">
                                                Modificar
                                            </button>
                                        </td>
                                        <td class="p-4 w-1/5">
                                        <button id="save-changes" onclick="eliminar(<%=lead.IDLead%>)" class="bg-red-500 hover:bg-red-700 text-white font-bold py-1 px-4 rounded">
                                            Eliminar
                                        </button></td>
                                    </tr>
                                    <% } else{%>
                                        <tr class="flex w-full mb-4 bg-gray-50">
                                            <td class="p-4 w-1/5">
                                                <% if (lead.NombreLead==""){%>
                                                   <p class="text-gray-400">Lead sin nombre</p>
                                                <% }else{%>
                                                    <%= lead.NombreLead %>
                                                <% }%>
                                            </td>
                                            <td class="p-4 w-1/5">
                                                <%  let fechaActual = lead.FechaPrimerMensaje;
                                                // Convertir la fecha a un objeto Date
                                                const fecha = new Date(fechaActual);
                                                // Obtener día, mes y año
                                                const dia = fecha.getDate().toString().padStart(2, '0');
                                                const mes = (fecha.getMonth() + 1).toString().padStart(2, '0'); // El mes comienza desde 0
                                                const año = fecha.getFullYear();
                                                // Crear la cadena en el formato deseado (dd/mm/yyyy)
                                                const fechaFormateada = `${dia}/${mes}/${año}`;%>
                                                <%= fechaFormateada %>
    
                                            </td>
                                            <td class="p-4 w-1/5">
                                                <%= lead.asignado_a %>
                                            </td>
                                            <td class="p-4 w-1/5">
                                                <button onclick="event.preventDefault(); modificar(<%=lead.IDLead%>)" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-4 rounded">
                                                    Modificar
                                                </button>
                                            </td>
                                            <td class="p-4 w-1/5">
                                            <button id="save-changes" onclick="eliminar(<%=lead.IDLead%>)" class="bg-red-500 hover:bg-red-700 text-white font-bold py-1 px-4 rounded">
                                                Eliminar
                                            </button></td>
                                        </tr>
                                    <% }row++} %>
                                </tbody>
                            </table>
                        </div>
                    </div>

                <%  }%> 
            </div>
        </div>
        </div>
    </div>
   <script src="/scripts/leads.js"></script>
   <script>
    const click = () => {
        console.log('click')
    }
    const eliminar = (IDLead) => {
    const csrf = document.getElementById('_csrf').value;
    fetch('Leads/eliminar', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'csrf-token': csrf,
        },
        body: JSON.stringify({IDLead: IDLead})
    }).then((result) => {
        return result.json()
    }).then((data) => {
      swal('Lead eliminado', 'El lead se ha eliminado de forma exitosa.', 'success');

      let html = "";
      let row=0;
      for (let lead of data.leads) {
        if(row%2==0){
        html += `
        <tr class="flex w-full mb-4">
            <td class="p-4 w-1/5">`;

            if (lead.NombreLead==""){
                html += `<p class="text-gray-400">Lead sin nombre</p>`
            }else{
                html += `${lead.NombreLead}`
            }
        html += `
            </td>
            <td class="p-4 w-1/5">`;
    
            let fechaActual = lead.FechaPrimerMensaje;
                    // Convertir la fecha a un objeto Date
            const fecha = new Date(fechaActual);
                    // Obtener día, mes y año
            const dia = fecha.getDate().toString().padStart(2, '0');
            const mes = (fecha.getMonth() + 1).toString().padStart(2, '0'); // El mes comienza desde 0
            const año = fecha.getFullYear();
                    // Crear la cadena en el formato deseado (dd/mm/yyyy)
            const fechaFormateada = `${dia}/${mes}/${año}`;
            
            html += `
                ${fechaFormateada}
            </td>
            <td class="p-4 w-1/5"> 
                ${lead.asignado_a}
            </td>
            <td class="p-4 w-1/5">
                <button onclick="event.preventDefault(); modificar(${lead.IDLead})" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-4 rounded">
                    Modificar
                </button>
            </td>
            <td class="p-4 w-1/5">
                <button id="save-changes" onclick="eliminar(${lead.IDLead})" class="bg-red-500 hover:bg-red-700 text-white font-bold py-1 px-4 rounded">
                    Eliminar
                </button>
            </td>
        </tr>`;
        }
        else{
            html += `
        <tr class="bg-gray-50 flex w-full mb-4">
            <td class="p-4 w-1/5">`;

            if (lead.NombreLead==""){
                html += `<p class="text-gray-400">Lead sin nombre</p>`
            }else{
                html += `${lead.NombreLead}`
            }
        html += `
            </td>
            <td class="p-4 w-1/5">`;
    
            let fechaActual = lead.FechaPrimerMensaje;
                    // Convertir la fecha a un objeto Date
            const fecha = new Date(fechaActual);
                    // Obtener día, mes y año
            const dia = fecha.getDate().toString().padStart(2, '0');
            const mes = (fecha.getMonth() + 1).toString().padStart(2, '0'); // El mes comienza desde 0
            const año = fecha.getFullYear();
                    // Crear la cadena en el formato deseado (dd/mm/yyyy)
            const fechaFormateada = `${dia}/${mes}/${año}`;
            
            html += ` ${fechaFormateada}
            </td>
            <td class="p-4 w-1/5"> 
                ${lead.asignado_a}
            </td>
            <td class="p-4 w-1/5">
                <button onclick="event.preventDefault(); modificar(${lead.IDLead})" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-4 rounded">
                    Modificar
                </button>
            </td>
            <td class="p-4 w-1/5">
                <button id="save-changes" onclick="eliminar(${lead.IDLead})" class="bg-red-500 hover:bg-red-700 text-white font-bold py-1 px-4 rounded">
                    Eliminar
                </button>
            </td>
        </tr>`;

        }
        row++;
    };

      document.getElementById('table_content').innerHTML = html;

  }).catch((err) => {
      console.log(err)
  })
  }

  // Almacena el ID del lead para modificar y poder enviarlo al servidor
  var leadId;
  // Función para modificar un lead
  function modificar(id) {
      leadId = id;
  console.log('modificar', id);
  $.get(`/lead/modificar/${id}`, function(data) {
      // Aquí, `data` es el lead que se obutvo del servidor. (Un botón de modificar)
      $('#nombre').val(data.NombreLead);
      
      //Se agregan los valores a los select porque no se pueden agregar directamente en el html

      // Aquí, se obtiene la fecha del primer mensaje y se formatea para que sea compatible con el input de tipo date.
      var fechaSeleccionada = new Date(data.FechaPrimerMensaje);
      var fechaFormateada = fechaSeleccionada.toISOString().split('T')[0];
      $('#fecha').val(fechaFormateada);

      $('#telefono').val(data.Telefono);

      $('#embudo').val(data.Embudo); 

        $('#etapa').val(data.Etapa);
        $('#status').val(data.Status);

        var archivadoSeleccionado = data.Archivado == 1 ? 'SI' : 'NO';
        $('#archivado').val(archivadoSeleccionado);

        var creadomanualSeleccionado = data.CreadoManual == 1 ? 'SI' : 'NO';
        $('#creadomanual').val(creadomanualSeleccionado); 

      $('#asignado_a').val(data.asignado_a);

      
        // Mostrar el modal
        $('#modal').show();

        // Agregar los controladores de eventos
        agregarControladoresDeEventos();
  });

  document.getElementById('botonGuardar').addEventListener('click', guardarCambios);
}

function agregarControladoresDeEventos() {
    // Agregar un controlador de eventos 'input' al campo de nombre
    $('#nombre').on('input', function() {
        // Verificar que el nombre solo contenga letras, caracteres especiales y emojis
        var regexNombre = /^[a-zA-ZñÑáéíóúÁÉÍÓÚüÜ\s.,!?¿¡\p{P}\p{S}]*$/u;
        if (!regexNombre.test(this.value)) {
            // Mostrar el mensaje de error
            $('#nombreError').text("Solo usar letras, caracteres especiales y emojis.");
            this.value = this.value.slice(0, -1); // Eliminar el último carácter
        } else {
            // Ocultar el mensaje de error
            $('#nombreError').text("");
        }
    });

    // Agregar un controlador de eventos 'input' al campo de teléfono
    $('#telefono').on('input', function() {
        // Verificar que el teléfono solo contenga números
        var regexTelefono = /^[0-9]*$/;
        if (!regexTelefono.test(this.value)) {
            // Mostrar el mensaje de error
            $('#telefonoError').text("Solo usar números.");
            this.value = this.value.slice(0, -1); // Eliminar el último carácter
        } else {
            // Ocultar el mensaje de error
            $('#telefonoError').text("");
        }
    });
  }

function cerrarModal() {
  $('#modal').hide();
  
    // Resetear los campos de entrada
    $('#nombre').val('');
    $('#telefono').val('');

    // Eliminar los controladores de eventos existentes
    $('#nombre').off('input');
    $('#telefono').off('input');
}

function crear() {
      
  console.log('crear');
  $('#modalCrear').show();
  // Agregar los controladores de eventos
  agregarControladoresDeEventosCrear();

  document.getElementById('botonGuardarCrear').addEventListener('clicks', guardarCambioscrear);
}

function agregarControladoresDeEventosCrear() {
    // Agregar un controlador de eventos 'input' al campo de nombre
    $('#nombreCrear').on('input', function() {
        // Verificar que el nombre solo contenga letras, caracteres especiales y emojis
        var regexNombre = /^[a-zA-ZñÑáéíóúÁÉÍÓÚüÜ\s.,!?¿¡\p{P}\p{S}]*$/u;
        if (!regexNombre.test(this.value)) {
            // Mostrar el mensaje de error
            $('#nombreErrorCrear').text("Solo usar letras, caracteres especiales y emojis.");
            this.value = this.value.slice(0, -1); // Eliminar el último carácter
        } else {
            // Ocultar el mensaje de error
            $('#nombreErrorCrear').text("");
        }
    });

    // Agregar un controlador de eventos 'input' al campo de teléfono
    $('#telefonoCrear').on('input', function() {
        // Verificar que el teléfono solo contenga números
        var regexTelefono = /^[0-9]*$/;
        if (!regexTelefono.test(this.value)) {
            // Mostrar el mensaje de error
            $('#telefonoErrorCrear').text("Solo usar números.");
            this.value = this.value.slice(0, -1); // Eliminar el último carácter
        } else {
            // Ocultar el mensaje de error
            $('#telefonoErrorCrear').text("");
        }
    });
  }


function cerrarModalCrear() {
  $('#modalCrear').hide();
  
    // Resetear los campos de entrada
    $('#nombreCrear').val('');
    $('#telefonoCrear').val('');

    // Eliminar los controladores de eventos existentes
    $('#nombreCrear').off('input');
    $('#telefonoCrear').off('input');
}

function guardarCambios() {
    // Recoge los datos del formulario
    var nombre = document.getElementById('nombre').value;
    var fecha = document.getElementById('fecha').value;
    var asignado_a = document.getElementById('asignado_a').value;
    var telefono = document.getElementById('telefono').value;
    var embudo = document.getElementById('embudo').value;
    var etapa = document.getElementById('etapa').value;
    var status = document.getElementById('status').value;
    var archivado = document.getElementById('archivado').value === 'true' ? 1 : 0;
    var creadomanual = document.getElementById('creadomanual').value === 'true' ? 1 : 0;

    // Crea un objeto con los datos del formulario
    var datos = {
        id: leadId,
        nombre: nombre,
        fecha: fecha,
        asignado_a: asignado_a,
        telefono: telefono,
        embudo: embudo,
        etapa: etapa,
        status: status,
        creadomanual: creadomanual,
        archivado: archivado
    };

// Envía los datos al servidor

$.ajax({
    url: '/lead/modificar',
    type: 'POST',
    data: JSON.stringify(datos),
    contentType: 'application/json',
    success: function(respuesta) {
        // Aquí puedes manejar la respuesta del servidor
        console.log(respuesta);
    }
}).then(data => {
    swal('Lead modificado', 'El lead se ha modificado de forma exitosa.', 'success')
    .then((value) => {
        if (value) { // Verifica si el usuario hizo clic en "OK"
            //hacemos que espere 5 segundos antes de redirigir
            setTimeout(() => {
                // Redirige al usuario a la página de leads
                window.location.href = '/lead/Leads';
            }, 10000);
        }
    }).catch((error) => {
        console.error('Error:', error);
        swal('Error', 'Ocurrió un error al guardar los cambios.', 'error');
    });
});
}


function guardarCambioscrear() {
    console.log("GuadaCambios");
    
    // Recoge los datos del formulario
    var nombre = document.getElementById('nombreCrear').value;
    var fecha = document.getElementById('fechaCrear').value;
    var asignado_a = document.getElementById('asignado_aCrear').value;
    var telefono = document.getElementById('telefonoCrear').value;
    var embudo = document.getElementById('embudoCrear').value;
    var etapa = document.getElementById('etapaCrear').value;
    var status = document.getElementById('statusCrear').value;
    var archivado = document.getElementById('archivadoCrear').value === 'true' ? 1 : 0;
    var creadomanual = document.getElementById('creadomanualCrear').value === 'true' ? 1 : 0;

    // Crea un objeto con los datos del formulario
    var datos = {
        nombre: nombre,
        fecha: fecha,
        asignado_a: asignado_a,
        telefono: telefono,
        embudo: embudo,
        etapa: etapa,
        status: status,
        creadomanual: creadomanual,
        archivado: archivado
    };

   // Envía los datos al servidor
    $.ajax({
        url: '/lead/crear',
        type: 'POST',
        data: JSON.stringify(datos),
        contentType: 'application/json',
        success: function(respuesta) {
            // Aquí puedes manejar la respuesta del servidor
            console.log(respuesta);
            console.log("AJAX");
            

            // Redirige al usuario a la página de leads
            window.location.href = '/lead/Leads';
        }
    });
}


  </script>
</body>
</html>